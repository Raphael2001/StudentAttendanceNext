import cms from "./requests/cms";
import site from "./requests/site";
import auth from "./requests/auth";
import server from "./requests/server";

const Api = {
  site,
  cms,
  auth,
  server,
};

export default Api;
